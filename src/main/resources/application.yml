spring:
  application:
    name: Jhatpat

  datasource:
    url: jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres?sslmode=require
    username: postgres.lgylilaimwflakkcasul
    password: Utkarsh1234@
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  data:
    redis:
      host: localhost
      port: 6379

  mail:
    host: smtp.gmail.com
    port: 587
    username: core.utkarsh@gmail.com
    password: cmuosggsvfprlmca

    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  cache:
    type: redis
    redis:
      time-to-live: 600000

  app:
    jwtSecret: myVeryStrongSecretKeyForJwtAuthentication123456
    jwtExpirationMs: 86400000

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    metrics:
      access: read_only